# A Plugins 0.4.1

这是一次面向官方商店提交路径的命名收口版本。

## 更新

- 插件 ID 从 `startup-optimizer` 改为 `a-plugins`。
- 手动安装目录改为 `<vault>/.obsidian/plugins/a-plugins/`。
- 宿主展示名继续保持 `A Plugins`。

## 注意

插件 ID 改动后，Obsidian 会把它视为不同于旧 `startup-optimizer` 构筑物的新插件目录。这是本次官方商店命名收口的预期结果。
